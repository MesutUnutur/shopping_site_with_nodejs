extends ../layouts/main-layout.pug 

include ../mixins/createProduct.pug

block content
    .row
        .col-lg-3
            include ../includes/menu.pug
        .col-lg-9
            .row 
                each product in productsımione
                    +createProduct(product)